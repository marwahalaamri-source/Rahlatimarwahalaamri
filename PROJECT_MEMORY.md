# ذاكرة مشروع «رحلتي» 🧠

> ملف المرجع السريع لحالة المشروع وقراراته. يُحدَّث مع كل مرحلة.
> التفاصيل الكاملة للهيكل في `docs/STRUCTURE.md`.
> **الهوية البصرية مُقفلة في `docs/DESIGN_SYSTEM.md`** — مرجعٌ ملزم لكل صفحة جديدة.
> **آخر تحديث:** ١٢ يوليو ٢٠٢٦ (بعد بناء صفحة «مشاريعي» المستقلة — الفصل العاشر)

---

## ١ — ما هو المشروع

موقع شخصي فاخر لمروه العامري (marwahalaamri@gmail.com) اسمه **«رحلتي»**:
نظام حياة متكامل — دفتر يوميات، تخطيط، أهداف، ذكريات — وليس تطبيق إنتاجية.
عربي بالكامل (RTL)، يعيش مع صاحبته ويتغيّر مع اليوم والوقت.

- **المستودع:** `marwahalaamri-source/Rahlatimarwahalaamri`
- **فرع العمل:** `claude/rahlati-homepage-design-b9s6ag`
- **معاينة الرئيسية:** https://claude.ai/code/artifact/0e3e6e3d-2c19-49d3-a7af-616711b64d1a
- **معاينة دفتري اليومي:** https://claude.ai/code/artifact/edbd3235-2739-42dd-b98b-23a32c3ad998
- **معاينة التخطيط الأسبوعي:** https://claude.ai/code/artifact/e5a54748-61f2-41ae-8bb0-de5f3c3a141c
- **معاينة الأهداف:** https://claude.ai/code/artifact/9f9a274f-6a55-4c2d-8fe9-47b602a91bb7

---

## ٢ — ما أُنجز ✅

| المرحلة | الحالة |
|---|---|
| الهوية البصرية الكاملة (ألوان، خطوط، زجاج، أزرار، حركة) | ✅ |
| الصفحة الرئيسية — لوحة حياة حية (سطح مكتب + جوال) | ✅ معتمدة من مروه |
| التحية الشخصية بالاسم تتغيّر حسب الوقت | ✅ |
| لمحة اليوم: تاريخ ميلادي + هجري، تركيز، تقدّم اليوم والأسبوع | ✅ |
| مكتبة ٤٣٢ اقتباسًا عربيًا (`assets/js/quotes.js`) + تغيير يدوي محفوظ لليوم | ✅ |
| نسخة الخميس التلقائية (ليلة القراءة + مكان صورة الكتاب) | ✅ |
| نسخة الجمعة التلقائية (سورة الكهف + روتين الجمعة) | ✅ |
| بطاقات الفصول الثمانية ببيانات حية + سطر «آخر نشاط» | ✅ |
| طبقة البيانات `rahlati:v2` + سجلّ الأقسام المعياري | ✅ |
| وثيقة الهيكل الشاملة `docs/STRUCTURE.md` | ✅ معتمدة |
| **نظام التصميم المُقفل `docs/DESIGN_SYSTEM.md`** (ألوان الوضعين، الخلفية السينمائية، المكوّنات) | ✅ معتمد هذه الجولة |
| صفحة دفتري اليومي `sections/journal.html` — تصميم UI/UX | ✅ (الحفظ الفعلي بانتظار اعتماد مروه) |
| طبقة بيانات مشتركة `assets/js/store.js` (تحميل/حفظ/ترقية آمنة) | ✅ |
| صفحة التخطيط الأسبوعي `sections/week.html` — مركز قيادة كامل | ✅ حفظ حقيقي |
| **صفحة الأهداف `sections/goals.html` — أربعة تبويبات (عادات🌱/أهداف🎯/مشاريعي🚀/أحلام✨)** | ✅ منجزة، حفظ حقيقي |
| قرار معماري: هرم الطموح الثلاثي (عادات🌱/أهداف🎯/أحلام✨) + تركيز الأسبوع | ✅ معتمد في `docs/STRUCTURE.md` §٢ |
| تصفير كل محتوى وهمي + طبقة بيانات مشتركة حقيقية عبر كل الصفحات | ✅ |
| `journal.html` موصولة بـ`rahlati:v2` (حفظ حقيقي + مرجع تركيز الأسبوع + ربط اختياري بهدف) | ✅ |
| `week.html`: قسم «تركيز الأسبوع» يحلّ محل `habitTracker` (إضافة/تأشير يومي/نسبة التزام) | ✅ |
| **بيانات مروه الحقيقية الكاملة مُدخَلة:** ٢٣ عادة، ١٤ هدفًا، ٢ مشروعًا، ٦ أحلام | ✅ منجز هذه الجولة |
| مشروع «رحلتي» (هذا الموقع) متتبَّع داخل تبويب مشاريعي بتقدّم حقيقي محسوب من خارطة الطريق | ✅ |
| الرئيسية: بطاقة «أهدافي» تلخّص المستويات الأربعة معًا | ✅ |
| **صفحة روتين الجمعة `sections/friday.html` — قلب النظام** (تأمل/امتنان/مراجعة مشتركة مع `week.review`/مراجعة حيّة للأهداف والعادات/تحضير روحي/اختيار تركيز وأولويات وتخطيط الأسبوع القادم) + `RahlatiStore.rolloverWeek()` لتدوير أسبوعي حقيقي | ✅ منجز |
| **تبديل المظهر 🌙/☀️ عبر كل الصفحات** (`assets/js/theme.js` + متغيرات CSS واحدة لكل صفحة، محفوظ في `localStorage`، انتقال ناعم ٣٠٠ مللي ثانية) | ✅ منجز |
| **صفحة صحتي `sections/health.html`** — الوزن (رسم بياني + هدف مرتبط تلقائيًا)، التغذية، الرياضة منخفضة التأثير، النوم، المكمّلات، العناية — فصلٌ تاسع جديد فوق الفصول الثمانية الأصلية، بطاقته الحية على الرئيسية، ومرتبط فعليًا بالعادات والأهداف والتخطيط الأسبوعي | ✅ منجز |
| **صفحة مشاريعي `sections/work.html`** — الفصل العاشر، ترقية تبويب «مشاريعي» من `goals.html` إلى صفحة مستقلة كاملة (نفس `store.projects[]`، بلا ازدواجية) بحقول جديدة: وصف، رؤية، خطوات تنفيذ، مهام، ملاحظات، روابط/ملفات، هدف/دخل متوقع، آخر نشاط لكل مشروع. `goals.html` عاد إلى ٣ تبويبات | ✅ منجز هذه الجولة |

## ٣ — ما بقي 🔜

**إنجاز هذه الجولة:** مروه أرسلت حياتها الحقيقية الكاملة — عادات الإيمان
والصحة والعلاقات، أهداف كل مجال (بما فيها هدف الوزن ٦٥←٥٢ كجم)، مشروعا
«رحلتي» و«بناء عمل تجاري خاص»، وستة أحلام كبرى. أُدخلت جميعها فعليًا في
`assets/js/store.js` (لا بيانات وهمية أبدًا — الحقول التأمّلية «لماذا؟»
و«رؤيتي» التي لم تكتبها مروه بنفسها تُركت فارغة عمدًا لتملأها هي). صفحة
الأهداف أُعيدت هيكلتها بالكامل إلى **أربعة تبويبات** حيّة قابلة للتفاعل:
تأشير عادات يومي/أسبوعي مع سلاسل حقيقية، أهداف بمعالمها، مشاريع بحالاتها
الثلاث (مستقبلي/حالي/مكتمل)، وأحلام كلوحة رؤية. تحقّقنا من كل شيء في
متصفح حقيقي: العدّادات، الحفظ، البقاء بعد إعادة التحميل، وانعكاس البيانات
على بطاقة الرئيسية.

**قرار توسعة معماري (هذه الجولة):** مروه طلبت صراحةً ثلاثة فصول إضافية
فوق الفصول الثمانية الأصلية المعتمدة — صحتي، العمل والمشاريع، الإحصائيات
— لتُبنى بالتتابع قبل مرحلة الربط النهائي. صحتي أول الثلاثة، مبنيّة
ومعروضة عليها؛ بانتظار مراجعتها قبل الانتقال للتالية.

1. **الإحصائيات `sections/stats.html`** — الفصل الحادي عشر، التالي مباشرةً
2. **اعتماد تصميم دفتري اليومي نهائيًا** (الحفظ مفعّل فعليًا، بانتظار
   موافقتها على البقية من التصميم)
3. رحلتي مع الإنجليزية `sections/english.html` (تكامل مع هدف «إتقان
   الإنجليزية» الموجود فعليًا في تبويب 🎯 أهداف)
4. إنجازاتي `sections/achievements.html` — **تُولَّد تلقائيًا من عادات
   وأهداف حقيقية مكتملة** (البيانات الحقيقية جاهزة الآن لتغذية هذا القسم)
5. ذكرياتي `sections/memories.html`
6. الإعدادات `sections/settings.html` (+ تصدير/استيراد JSON + صورة كتاب الخميس)
7. استخراج الهوية البصرية المشتركة إلى `assets/css/rahlati.css` (الـCSS لا يزال مكررًا
   بين الصفحات؛ طبقة البيانات باتت مشتركة فعلًا عبر `assets/js/store.js`)
8. صورة كتاب الأسبوع: مروه سترسلها لاحقًا لوضعها في نسخة الخميس
   (روتين الخميس نفسه يعيش داخل الرئيسية فقط، لا صفحة مستقلة له)
9. **توسعات مستقبلية مذكورة صراحة:**
   - ربط أهداف الأسبوع (`week.weeklyGoals`) بأهداف «أهدافي» طويلة المدى
   - ربط مهام الهدف بمهام الأسبوع الفعلية
   - ربط الأحلام بمشاريعها عبر `relatedGoalIds`/`linkedDreamId` (حلم
     «بناء عمل تجاري ناجح» ↔ مشروع «بناء عملي التجاري الخاص»)
   - تتبّع رقمي لهدف الوزن (٦٥→٥٢ كجم) بدل نسبة تقدّم يدوية فقط

## ٤ — ترتيب الصفحات

الرئيسية `/index.html` ثم في القائمة (RTL):
**الرئيسية · يومياتي · أسبوعي · الجمعة · أهدافي · الإنجليزية · إنجازاتي · ذكرياتي · الإعدادات · صحتي · مشاريعي · الإحصائيات**

ترقيم الفصول على الرئيسية: ٠١ يومياتي، ٠٢ أسبوعي، ٠٣ روتين الجمعة، ٠٤ أهدافي،
٠٥ الإنجليزية، ٠٦ إنجازاتي، ٠٧ ذكرياتي، ٠٨ الإعدادات، ٠٩ صحتي، ١٠ مشاريعي،
١١ الإحصائيات (قيد الإنشاء).

---

## ٥ — قواعد التصميم (ثابتة)

- **الإحساس:** فخامة، هدوء، أناقة، أنثوي ناضج — مزيج Apple / مجلة فاخرة / دفتر شخصي
- **الخلفية:** صورة سينمائية حقيقية واحدة (`assets/hero.jpg` — جبال وشروق وطريق)
  مثبّتة خلف كل الصفحات، والمحتوى ينساب فوقها بطبقات زجاجية
- **ممنوع:** رسومات فيكتور/كرتونية، مظهر لوحة تحكم جافّ، ازدحام، letter-spacing على العربية
- **الشعار** (`assets/logo.png` بخلفية شفافة): مركز الهوية، يُحترم كما هو
- **الزجاج:** `rgba(16,26,48,.40)` + blur 10–14px + حدود `rgba(201,178,140,.2)`
  + ظل `0 18px 50px rgba(8,12,24,.26)` + خط ضوء داخلي علوي + زوايا 26–34px
- **الأزرار:** الرئيسي ذهبي متدرّج `#e3cfa5→#b3986e` بلمعة تمرّ عند hover؛
  الثانوي شفاف بحدود `rgba(248,244,236,.26)`
- **الأشرطة:** 3px ذهبية تمتلئ بحركة 1.4s عند الظهور
- **الحركة:** fade-up هادئ (IntersectionObserver + .reveal)، احترام `prefers-reduced-motion`
- **الأرقام:** عربية مشرقية دائمًا (٠١٢٣٤٥٦٧٨٩) — دالة `arabicNum`
- **الزخرفة:** النجمة الرباعية ✦ من الشعار فقط
- **الأسبوع يبدأ السبت** (س ح ن ث ر خ ج)
- **التواريخ:** ميلادي + هجري (أم القرى) معًا
- **الجوال أولوية قصوى:** يُصمَّم كتطبيق آيفون فاخر (أهداف لمس واسعة، عمود واحد)

## ٦ — الألوان

| الرمز | القيمة | الاستخدام |
|---|---|---|
| `--navy-950` | `#0d1426` | أرضية الصفحة |
| `--navy-900` | `#101a30` | تدرّجات داكنة |
| `--navy-800` | `#16223d` | الزجاج والبطاقات |
| `--gold` | `#c9b28c` | الذهبي الأساسي (أزرار، فواصل، أيقونات) |
| `--gold-deep` | `#b3986e` | نهايات التدرّج الذهبي |
| `--gold-soft` | `#e8dcc6` | نصوص التمييز الفاتحة |
| `--ivory` | `#f8f4ec` | النص الأساسي |
| hairline | `rgba(201,178,140,.22)` | الحدود والفواصل |

## ٧ — الخطوط

- **Amiri** (400/700 + مائل) — العناوين، الأرقام الكبيرة، الاقتباسات، كلمة اليوم الإنجليزية (مائل)
- **Almarai** (300/400/700) — النصوص والواجهات؛ الوزن 300 هو الافتراضي للنصوص
- المصدر: Google Fonts (وفي معاينات الـArtifact تُضمَّن woff2 داخل الملف)

---

## ٨ — القرارات المتفق عليها

1. **التقنية:** HTML/CSS/JS خام بلا أدوات بناء — يعمل على GitHub Pages وبفتح الملف مباشرة
2. **البنية معيارية:** سجل أقسام `SECTIONS` + مخزن واحد `rahlati:v2` في localStorage؛
   إضافة قسم جديد = عنصر بالسجل + مفتاح بالمخزن + صفحة — دون إعادة تصميم
3. **الترقية الآمنة:** أي تغيير بالمخطط يرفع النسخة مع دالة ترحيل — لا فقدان بيانات
4. **الرئيسية مركز النظام:** كل قسم يغذّيها ببيانات حية + «آخر نشاط» لكل بطاقة
5. **سير العمل الافتراضي:** تصميم UI/UX أولًا ← اعتماد مروه ← ربط الحفظ — لكل قسم؛
   إلا إذا طلبت مروه صراحةً حفظًا حقيقيًا من البداية (كما في التخطيط الأسبوعي)
6. **البيانات التجريبية** تبقى حتى يُربط كل قسم فعليًا، وتُستبدل ببيانات مروه الحقيقية
6.١ **الحفظ الفعلي:** `assets/js/store.js` يوفّر `load()/save()/touch()` مشتركة؛
   كل قسم يستدعيها مباشرة (autosave مع مؤشر «✓ محفوظ تلقائيًا» صغير) بدل زر حفظ يدوي،
   إلا صفحة اليوميات التي لا تزال بانتظار الاعتماد
7. **التحية:** «أهلًا، مروه العامري 🌿» وتتغيّر حسب الوقت (صباح النور/نهارك سعيد/مساء الورد/ليلتك هادئة)
8. **الاقتباس اليومي:** حتمي حسب التاريخ يدور على ٤٣٢ اقتباسًا؛ التغيير اليدوي يُحفظ لبقية اليوم في `rahlati:quote`
9. **معاينة أيام الخميس/الجمعة:** `?preview=thu|fri` أو `#preview=thu|fri`
10. **الحفظ في دفتري اليومي غير مفعّل بعد** — زر الحفظ يعرض توست، والربط بعد الاعتماد
11. الأوسمة ستُفتح تلقائيًا وفق قواعد القسم ٩ في `docs/STRUCTURE.md`
11.١ ~~تصنيفات وحالات الأهداف بثماني تصنيفات مسطّحة (منها «أحلام»)~~ —
   **استُبدل** بالقرار ١٣ أدناه (هرم ثلاثي)؛ الحالات الأربع للأهداف
   (لم يبدأ/جارٍ التنفيذ/متوقف/مكتمل) ما زالت سارية على تبويب 🎯 أهداف فقط
12. لقطات وتحقق كل مرحلة عبر متصفح حقيقي (سطح مكتب 1440px + جوال 390px) قبل الدفع
13. **هرم الطموح ثلاثي المستويات (قرار جوهري):** عادات 🌱 (متكرّرة، سلسلة
    أيام) ← أهداف 🎯 (متوسطة المدى، نسبة تقدّم ومعالم) ← أحلام ✨ (كبرى، رؤية
    بلا ضغط تاريخ). الثلاثة يعيشون في صفحة واحدة `goals.html` بثلاثة تبويبات،
    لا ثلاث صفحات منفصلة — الرئيسية تبقى ثمانية فصول دون تضخّم
14. **تركيز الأسبوع (`weeklyFocus`):** بؤرة أو بؤرتان (حد أقصى ٢) تُختاران
    كل جمعة، تظهران تلقائيًا في الرئيسية + اليوميات + الأسبوعي من مصدر واحد
    دون إعادة كتابة، مع حساب نسبة الالتزام تلقائيًا نهاية الأسبوع. يحلّ محل
    `week.habitTracker` المفرد الذي بُني سابقًا. تفاصيل المخطط الكامل في
    `docs/STRUCTURE.md` §٥
15. **لا محتوى وهمي بعد الآن (قرار صارم):** `assets/js/store.js` يبني مخزنًا
    فارغًا تمامًا (`buildEmptyStore`) بلا أي هدف/عادة/تدوينة/إنجاز/ذكرى
    مختلَقة — فقط سقالات بنيوية محايدة (كخانات أولويات فارغة أو خطوات
    روتين الجمعة التي وصفتها مروه بنفسها). كل صفحة تعرض حالة فارغة أنيقة
    بدل بيانات وهمية حتى تُدخِل مروه بياناتها الحقيقية
16. **مصدر بيانات واحد فعليًا، لا نظريًا:** `journal.html` كانت الصفحة
    الوحيدة غير الموصولة بـ`rahlati:v2` (تعمل على كائن تجريبي منفصل في
    الذاكرة) — أُصلح ذلك؛ الآن تقرأ/تكتب من نفس المخزن، وتعرض تركيز
    الأسبوع الحالي بعلامة «التزمتُ اليوم؟»، وتتيح ربط التدوينة اختياريًا
    بهدف نشط من `goals.html`. مخطط اليوميات وُحِّد أيضًا في `store.js`
    (`diary` بدل `text`، `gratitude`/`priorities` كمصفوفات) مع ترحيل آمن
    للشكل القديم
17. **`week.html`:** قسم «المتابعة» (عادة مفردة + سلسلة) استُبدل بقسم
    «🎯 تركيز الأسبوع» الفعلي — إضافة/حذف حتى عنصرين، تأشير يومي بصف أيام
    قابل للّمس، ونسبة التزام تُحسب تلقائيًا وتظهر في مراجعة نهاية الأسبوع
18. **مستوًى رابع: مشاريعي 🚀 (قرار من مروه):** منفصل عن الأهداف —
    «هذا القسم لا علاقة له بوظيفتي اليومية؛ هو لأفكاري ومشاريعي الشخصية».
    حالات مبسّطة (مستقبلي/حالي/مكتمل) بلا تصنيف مجال حياة، بنفس بنية
    الأهداف (تقدّم/معالم/مهام/صور). `assets/js/store.js` يحوي دالة
    `project()` مقابل `goal()`/`habit()`/`dream()`
19. **بيانات مروه الحقيقية هي المحتوى الابتدائي الآن (v3):** مفتاح
    التخزين رُقّي من `rahlati:v2` إلى `rahlati:v3` لضمان تحميلها نظيفةً في
    أي متصفح جرّب النسخة الفارغة سابقًا (لا خطر فقدان بيانات — لا بيانات
    حقيقية أُدخلت بعد من مروه نفسها في تلك المرحلة). الحقول التأمّلية
    («لماذا؟»، «رؤيتي») التي لم تكتبها مروه بنفسها تُركت فارغة عمدًا —
    لا اختلاق مشاعر أو أسباب نيابةً عنها؛ الحقائق فقط (الأسماء، التصنيفات،
    الأرقام كوزنها الحالي والمستهدف) أُدخلت
20. **تكرار عمدي، لا خطأ:** بطاقة عادة «قراءة سورة الكهف/التخطيط الأسبوعي»
    موجودة في تبويب 🌱 عادات (لتتبّع السلسلة عبر أسابيع طويلة) وأيضًا في
    قائمة `friday.checklist` (كخانة إنجاز أسبوعية واحدة) — الاثنان يخدمان
    غرضين مختلفين ولا يوجد تضارب بينهما
21. **روتين الجمعة هو قلب النظام (قرار من مروه):** ليس قائمة تحقّق، بل لحظة
    تدوير أسبوعي كاملة. `sections/friday.html` يضيف حقولًا مرحلية جديدة في
    `store.friday` (`gratitude`, `spiritualPrep`, `nextWeekGoal`,
    `nextWeekPriorities`, `nextWeekFocusItems`, `scheduleNotes`,
    `lastCompleted`) بالإضافة لدالة `RahlatiStore.rolloverWeek(store, force)`:
    عند `force=true` (زر «أنهيتُ الأسبوع») تُؤرشف الأسبوع المنتهي في
    `weekHistory[]`/`weeklyFocus.history[]` وتُبنى بيانات الأسبوع الجديد من
    الحقول المرحلية فورًا؛ عند `force=false` تُستدعى تلقائيًا من `loadStore()`
    كفحص أمان إن فات موعد ضغط الزر. مراجعة نهاية الأسبوع تشارك نفس كائن
    `week.review` مع القسم المختصر في `week.html` — لا ازدواجية بيانات، فقط
    بابان لدخوله. لأن الرئيسية واليوميات والأسبوعي تقرأ من نفس المصدر، ينعكس
    التدوير عليها تلقائيًا دون أي تعديل إضافي في تلك الملفات
22. **تصحيح منطقي أثناء البناء:** أول تنفيذ لـ`rolloverWeek(force=true)` كان
    يحسب الأسبوع الجديد بـ`currentWeekStart()` مباشرةً — وهذا خطأ لأن الزر
    يُضغط عادة يوم الجمعة، قبل أن يتغيّر التاريخ الحقيقي فعليًا؛ فكانت تعيد
    بناء نفس الأسبوع الجاري بدل الأسبوع القادم. صُحِّح ليكون دومًا
    «endingWeekStart + ٧ أيام» بصرف النظر عن تاريخ اليوم الفعلي
23. **تبديل المظهر عبر متغيرات CSS واحدة، لا نسختين من التصميم:** كل صفحة
    تحمل نفس أسماء المتغيرات (`--navy-950/900/800`, `--ivory`, `--gold-soft`,
    `--glass`, `--hairline`) بالإضافة لخمسة متغيرات "خام" جديدة تحمل مكوّنات
    RGB بدل لون كامل (`--ink-rgb`, `--paper-rgb`, `--glass-rgb`,
    `--shadow-rgb`, `--accent-navy-rgb`) لتُستخدم داخل `rgba(var(--x-rgb),
    ألفا)` في كل مكان كانت فيه قيم `rgba()` حرفية متكرّرة (أشرطة التنقّل،
    الخلفيات الشفافة، النصوص الثانوية). قيم اللون الذهبي (`--gold`,
    `--gold-deep`) **لا تتغيّر بين الوضعين إطلاقًا** — بقرار صريح من مروه أن
    الهوية الذهبية تبقى واحدة. الوضع الفاتح يُفعَّل بإضافة
    `data-theme="light"` على `<html>` (عبر `:root[data-theme="light"]` في
    كل ملف)، محفوظ في `localStorage["rahlati:theme"]`، وزر التبديل
    (🌙/☀️) مبنيّ في `assets/js/theme.js` المشترك بين كل الصفحات. سكربت
    صغير مضمّن في أول `<head>` (قبل أي CSS) يقرأ التفضيل المحفوظ ويضبط
    `data-theme` فورًا لمنع "وميض" الوضع الخاطئ عند التحميل. انتقال ناعم
    ٣٠٠ مللي ثانية عبر قاعدة `*{transition:...}` عامة (لا تكسر أي تأثيرات
    hover/transform موجودة مسبقًا لأنها أضعف تخصيصًا وتُستبدَل تلقائيًا حيث
    كان هناك `transition` مخصّص من قبل)
24. **صحتي — فصل تاسع جديد، مرتبط لا مواز:** مروه طلبت صراحةً توسعة الشبكة
    من ٨ فصول إلى ١١ (صحتي + العمل والمشاريع + الإحصائيات). صحتي مبنيّة
    بحيث لا تُنشئ نظام تتبّع منفصلًا عن العادات/الأهداف الموجودة أصلًا: كل
    تسجيل (وزن، تمرين، تغذية، نوم، مكمّل) يكتب مباشرةً في `habits[]`/`goals[]`
    الحقيقية (تفاصيل الربط في `docs/STRUCTURE.md` §٥). الوزن الحالي ٦٥ كجم
    والمستهدف ٥٢ كجم استُخدما كما ذكرتهما مروه بالضبط (ليسا بيانات وهمية)،
    وكذلك المكمّلات الأربعة (Berberine, Vitamin C, Glutathione, Vitamin D)
25. **تصحيح أثناء البناء — أساس ثابت لتقدّم الوزن:** أول تنفيذ حسب نسبة
    التقدّم بالاعتماد على أول عنصر في `weight.log[]` كـ"البداية" — لكن بما
    أن العنصر الأول يُزرَع بتاريخ اليوم، فإن أي تسجيل وزن جديد في نفس اليوم
    كان يستبدل ذلك العنصر نفسه (لا يُضاف عنصر ثانٍ لنفس التاريخ)، فتُصبح
    البداية والحالي متطابقين دائمًا ونسبة التقدّم عالقة عند ٪٠. صُحِّح بإضافة
    حقل `weight.startWeight` منفصل وثابت لا يتأثر بتحديثات `log[]`
26. **تصحيح هوية بصرية (ملاحظة من مروه):** بعد إضافة الوضع الفاتح، شعرت
    مروه أن الموقع "بدأ يفقد هويته الأصلية" — الخلفية السينمائية (الطريق بين
    الجبال) لم تعد تظهر بوضوح. الفحص أثبت أن `--glass` في الوضع الفاتح كانت
    `rgba(255,255,255,.82)` — شفافية عالية جدًا أخفت الصورة خلف البطاقات
    المتراصّة وجعلت الصفحات تبدو كلوحة تحكّم بيضاء. صُحِّحت إلى
    `rgba(255,255,255,.5)` عبر كل الصفحات الست بعد تحقّق بصري حقيقي (لقطات
    فعلية أثناء التمرير، لا لقطات `fullPage` من Playwright التي قد تُظهر
    الخلفية الثابتة بشكل مضلِّل بسبب طريقة تجميعها للقطات الطويلة)
27. **إنشاء `docs/DESIGN_SYSTEM.md` — قرار حاكم:** بطلب صريح من مروه، تم
    إقفال الهوية البصرية في وثيقة واحدة ملزمة (ألوان الوضعين، الخلفية
    السينمائية، الطباعة، المكوّنات الجاهزة، الحركة) قبل متابعة أي صفحة
    جديدة. من الآن: لا تُضاف صفحة إلا بنسخ اللبنات من تلك الوثيقة حرفيًا؛
    أي تغيير في القيم يُحدَّث فيها أولًا ثم يُعمَّم على كل الصفحات معًا —
    لا مزيد من الانحراف التدريجي بين الصفحات
28. **تطوير `health.html` إلى لوحة إحصاءات دون تغيير الهوية:** بطلب صريح من
    مروه، أُضيفت: بطاقة «صحة اليوم» (مؤشر صحة محسوب من ٥ عناصر: بروتين/ماء/
    مكمّلات/نشاط رياضي اليوم + نوم الليلة الماضية، بنمط `.ring-box` نفسه
    المستخدَم في `week.html`)، معدّل النزول الشهري وآخر ٥ قياسات وزن، أشرطة
    تقدّم للبروتين والماء مع «المتبقي للهدف»، إحصاءات أسبوعية للرياضة
    (إجمالي الدقائق، عدد جلسات كل نوع، أطول سلسلة التزام)، متوسط أوقات
    النوم/الاستيقاظ ورسم شريطي بسيط لآخر ٧ ليالٍ، ووقت جرعة + سلسلة التزام
    + نسبة شهرية + تاريخ آخر جرعة لكل مكمّل (حقل `doseTime` جديد في
    `store.health.supplements[]`). **كل إيموجي داخل البطاقات استُبدل بأيقونات
    خط ذهبية LineIcon** (قاعدة جديدة في `DESIGN_SYSTEM.md` §٤) — الإيموجي
    الوحيد المتبقّي هو زرّ تبديل المظهر 🌙/☀️ وإيموجي الترحيب الزخرفي، وكلاهما
    مستثنى صراحةً. لم يتغيّر أي لون أو خلفية أو تخطيط قائم — إضافة صرفة فوق
    نفس الصفحة
29. **مشاريعي تتخرّج من تبويب إلى صفحة مستقلة (قرار من مروه):** تبويب
    «🚀 مشاريعي» داخل `goals.html` صار صفحة `sections/work.html` الكاملة —
    **نفس** `store.projects[]` بلا نسخ أو ازدواجية، فقط واجهة أغنى. حقول
    جديدة أُضيفت للمخطط: `description` (وصف قصير)، `links[]` (روابط/ملفات
    بتسمية اختيارية)، `expectedIncome` (نص حر)، و`lastActivity` **لكل
    مشروع على حدة** (يُحدَّث تلقائيًا عند أي تعديل على ذلك المشروع تحديدًا،
    مختلف عن `activity.work` العام للصفحة). حقل `why` الموجود أصلًا أُعيد
    تسميته في الواجهة فقط إلى «الرؤية» دون تغيير في المخطط. بعد النقل،
    `goals.html` عاد إلى ٣ تبويبات (عادات/أهداف/أحلام) — الأمثلة الواقعية
    المتاحة هي فقط ما أدخلته مروه فعليًا («رحلتي» و«بناء عملي التجاري
    الخاص»)؛ أفكار مثل English Journey أو منتجات رقمية لم تُضَف كبيانات
    لأنها لم تُقرَّر بعد — تُضاف حين تفعل مروه ذلك بنفسها

## ٩ — ملفات المشروع الحالية

```
index.html                 الرئيسية (لوحة الحياة الحية)
sections/journal.html      دفتري اليومي (UI جاهز، الحفظ لاحقًا)
sections/week.html         التخطيط الأسبوعي (مركز قيادة كامل، حفظ حقيقي)
sections/goals.html        أهدافي — ٤ تبويبات (عادات/أهداف/مشاريعي/أحلام)، بيانات مروه الحقيقية
sections/friday.html       روتين الجمعة — قلب النظام، تدوير أسبوعي حقيقي
sections/health.html       صحتي — وزن/تغذية/رياضة/نوم/مكمّلات/عناية، مرتبطة بالعادات والأهداف
sections/work.html         مشاريعي — أفكاري ومشاريعي الشخصية، منفصلة عن goals.html الآن
assets/hero.jpg            الخلفية السينمائية (أصل مروه)
assets/logo.png            الشعار بخلفية شفافة (من أصل مروه)
assets/favicon.png         أيقونة التبويب
assets/memory-sample.jpg   صورة تجريبية لبطاقة الذكريات
assets/js/quotes.js        مكتبة ٤٣٢ اقتباسًا
assets/js/store.js         طبقة البيانات المشتركة (rahlati:v2 — تحميل/حفظ/ترقية آمنة)
assets/js/theme.js         تبديل المظهر الداكن/الفاتح المشترك بين كل الصفحات
docs/STRUCTURE.md          وثيقة الهيكل الشاملة (المعتمدة)
docs/DESIGN_SYSTEM.md      نظام التصميم المُقفل — مرجع ملزم لكل صفحة جديدة
PROJECT_MEMORY.md          هذا الملف
```
